# =========================================================
# DJANGO CORE
# =========================================================
DJANGO_ENV=production
DEBUG=False

SECRET_KEY=+17bg+3_gqu5eoknx&vv3#fkc&2f-q9l$n$n!$&7nu7t^&%wtu

ALLOWED_HOSTS=api.pdinfinita.app,pdinfinita.app,dev.pdinfinita.app

CSRF_TRUSTED_ORIGINS=https://pdinfinita.app,https://api.pdinfinita.app,https://dev.pdinfinita.app

SECURE_PROXY_SSL_HEADER=HTTP_X_FORWARDED_PROTO,https
SECURE_SSL_REDIRECT=True

SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True

SECURE_HSTS_SECONDS=31536000
SECURE_HSTS_INCLUDE_SUBDOMAINS=True
SECURE_HSTS_PRELOAD=True

SECURE_CONTENT_TYPE_NOSNIFF=True
SECURE_BROWSER_XSS_FILTER=True
SECURE_REFERRER_POLICY=strict-origin-when-cross-origin

# =========================================================
# DATABASE (POSTGRESQL)
# =========================================================
POSTGRES_DB=pdinfinita_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=pdadmin@2026
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5433

DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
#DATABASE_URL=postgresql://postgres:pdadmin%402026@127.0.0.1:5433/pdinfinita_db

# =========================================================
# REDIS / CELERY
# =========================================================
REDIS_HOST=127.0.0.1
REDIS_PORT=6380
REDIS_DB=0
REDIS_PASSWORD=
REDIS_URL=redis://${REDIS_HOST}:${REDIS_PORT}/${REDIS_DB}
CELERY_ENABLED=True 
CELERY_BROKER_URL=${REDIS_URL}
CELERY_RESULT_BACKEND=${REDIS_URL}

# =========================================================
# CORS
# =========================================================
CORS_ALLOW_ALL_ORIGINS=False
CORS_ALLOWED_ORIGINS=https://pdinfinita.app,https://developer.pdinfinita.app

# =========================================================
# EMAIL (ZOHO)
# =========================================================
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.zoho.eu
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
EMAIL_HOST_USER=suporte@velomacontabilidade.com
EMAIL_HOST_PASSWORD=Janeiro_32Janeiro_32
DEFAULT_FROM_EMAIL=suporte@velomacontabilidade.com
EMAIL_TIMEOUT=30

# =========================================================
# URLS
# =========================================================
PUBLIC_BASE_URL=https://api.pdinfinita.app
FRONTEND_BASE_URL=https://pdinfinita.app
AUTH_RESET_PASSWORD_PATH=/reset-password

# =========================================================
# LOGIN SECURITY
# =========================================================
LOGIN_ALERT_ENABLED=True
LOGIN_ALERT_GEOLOOKUP_ENABLED=True
LOGIN_ALERT_GEOLOOKUP_URL=https://ipapi.co/{ip}/json/
LOGIN_ALERT_GEOLOOKUP_TIMEOUT=3

# =========================================================
# ENCRYPTION
# =========================================================
USER_PROFILE_ENCRYPTION_KEY=CHANGE_THIS_WITH_FERNET_KEY

# =========================================================
# MINIO / S3 STORAGE
# =========================================================
MINIO_ROOT_USER=admin@pdinfinita.app
MINIO_ROOT_PASSWORD=pdadmin@2026

AWS_ACCESS_KEY_ID=${MINIO_ROOT_USER}
AWS_SECRET_ACCESS_KEY=${MINIO_ROOT_PASSWORD}

AWS_STORAGE_BUCKET_NAME=pdinfinita-storage
AWS_S3_ENDPOINT_URL=http://127.0.0.1:9002

AWS_S3_REGION_NAME=us-east-1
AWS_S3_SIGNATURE_VERSION=s3v4

AWS_S3_ADDRESSING_STYLE=path

AWS_S3_OBJECT_PARAMETERS={"CacheControl": "max-age=86400"}

DEFAULT_FILE_STORAGE=storages.backends.s3boto3.S3Boto3Storage
STATICFILES_STORAGE=storages.backends.s3boto3.S3Boto3Storage